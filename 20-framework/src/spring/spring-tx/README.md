## package
org.springframework
```
dao
jca
    cci
    context
    endpoint
    support
    work
transaction
    annotation
        Isolation 4种隔离级别
        Propagation 7种传播范围
    config
    event
    interceptor
        TransactionInterceptor
        TransactionProxyFactoryBean
    jta
    support
        TransactionCallback
        TransactionTemplate
    PlatformTransactionManager
    SavepointManager
    TransactionDefinition
    TransactionStatus
```

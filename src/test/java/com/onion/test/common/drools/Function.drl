package com.onion.test.drools;

import java.text.*;
import java.util.*;

function String formatDate(Date date) {
    SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
    return sdf.format(date);
}